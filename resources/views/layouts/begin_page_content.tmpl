{{ define "layouts/begin_page_content.tmpl" }}
    <!-- Begin Page Content -->
    <div class="container-fluid">

        <!-- Page Heading -->
        <div class="align-items-center justify-content-between mb-4">
            <h1 class="h3 text-gray-800">{{ .pageheading }}</h1>
        </div>
{{end}}